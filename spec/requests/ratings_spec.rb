require "rails_helper"

# Scaffold-generated CRUD is superseded by assignment_api_spec.rb

RSpec.describe "/ratings (scaffold)" do
  it "is documented in assignment_api_spec" do
    expect(Rating).to be < ApplicationRecord
  end
end
